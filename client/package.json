{
  "name": "arcagrad-client",
  "private": true,
  "version": "0.1.0",
  "description": "Native (Tauri v2) client for arcagrad — reuses the web/ SvelteKit UI.",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
